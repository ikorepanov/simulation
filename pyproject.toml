[project]
name = "simulation"
version = "0.1.0"
description = "The second project of Sergey Zhukov's roadmap"
authors = [{ name = "Ilia Korepanov" }]

dependencies = []

requires-python = ">=3.9, <3.12"
readme = "README.md"
license = { file = "LICENSE" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
simulation = "simulation:main"
